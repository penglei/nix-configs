#zmodload zsh/zprof

fpath+=~/.zfunc
path+="$HOME/Library/Application Support/edgedb/bin"
path+="$HOME/.moon/bin"
path+="$HOME/.npm-global/bin"
path+="/opt/ocaml/bin"
path+="/Applications/Racket v8.16/bin"
path+="/opt/zulujdk/current/bin"

export JAVA_HOME=/opt/zulujdk/current/zulu-17.jdk/Contents/Home

eval "$(opam env)"

function login-argocd() {
  ssh -t jumper ssh -t khaos ssh -t lespeng@khaos-argocd
}

if [ -x ~/.keys.sh ];then
  source ~/.keys.sh
fi

#source $HOME/.ghcup/env
